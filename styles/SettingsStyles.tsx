import { StyleSheet } from 'react-native'

export const settings = StyleSheet.create({
  outerContainer: {
    flex: 1,
    flexDirection: 'column',
    padding: 0,
    marginTop: 2,
    marginBottom: 10,
    marginHorizontal: 6,
  },
})
