export const TEXTS = {
  //every fontsize goes through this function
  //due to differing sizes this'll make life a billion times easier if we switch fonts
  adjust: (inputSize:any) => {
    return inputSize * 1
  },
};